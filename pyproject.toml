[project]
name = "ironforge"
version = "0.2.0"
description = "Evidence-based workout program designer"
requires-python = ">=3.11"
dependencies = ["flask>=3.0"]

[project.scripts]
ironforge = "ironforge.cli:main"
ironforge-web = "ironforge.web:run"
